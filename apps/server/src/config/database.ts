import fs from "fs";
import path from "path";

import { Sequelize } from "sequelize";
import { env } from "./env";

const environment = env.NODE_ENV;

const seedDevelopmentDatabase = () => {};

export const initializeDatabase = () => {};
